@inject IDbContextFactory<ScanContext> scanDbFactory

@code{
    protected ScanContext scanDb;
    

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();

        if (scanDb == null)
        {
            scanDb = await scanDbFactory.CreateDbContextAsync();
        }

    }
}