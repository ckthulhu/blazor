@page "/Scan"
@inherits DbContextPage

<PageTitle>Scan</PageTitle>



<RadzenText TextStyle="TextStyle.H3">Scan</RadzenText>

<RadzenLink Path="@("/Scan/New")" >
    <RadzenButton ButtonStyle="ButtonStyle.Success">
        New App
    </RadzenButton>
</RadzenLink>
@if (scanRequestList != null)
{
    <RadzenDataGrid Data="scanRequestList">
        <Columns>
            <RadzenDataGridColumn Property="@nameof(ScanRequest.ScanRequestId)" Title="Scan ID" />
            <RadzenDataGridColumn Property="@nameof(ScanRequest.DeploymentLocation)" Title="Deployment" />


            <RadzenDataGridColumn>
                <Template Context="data">
                    <RadzenLink Path="@("/Scan/Edit/" + data.ScanRequestId)">
                        <RadzenButton>
                            Edit
                        </RadzenButton>
                    </RadzenLink>

                </Template>
            </RadzenDataGridColumn>

        </Columns>

    </RadzenDataGrid>
}




@code {
    IQueryable<ScanRequest> scanRequestList;

    //List<WebApp> webAppList;

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();

        scanRequestList = scanDb.ScanRequests;

        var test = "";

    }


    

}

